{
  "name": "@q2w/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests in shared\" && exit 0",
    "lint": "echo \"lint: shared\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
